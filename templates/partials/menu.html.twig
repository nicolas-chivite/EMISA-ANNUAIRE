{% if app.user %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        {# <a class="navbar-brand" href="/">Gestion de l'annuaire EMISA</a> #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse p-2 mx-3" id="navbarNav">
            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion annuaire</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('app_course_index') }}">Gestion des formations</a></li>
                    <li><a class="dropdown-item" href="{{ path('app_student_index') }}">Gestion des étudiants</a></li>
                </ul>
            </div>
            <div class="nav-item dropdown p-2 mx-2">
                <a class="nav-link dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">Gestion site internet</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('app_page_index') }}">Gestion des pages</a></li>
                    <li><a class="dropdown-item" href="{{ path('app_menu_index') }}">Gestion des menus</a></li>
                </ul>
            </div>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_message_index') }}">Gestion des messages</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_index') }}">Gestion des comptes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_log_index') }}">Logs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-danger btn-sm" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
{% endif %}